server:
  port: 8001

spring:
  application:
    name: spring-ai-chat

# 基于openAI标准接口进行聊天
# 这里使用的是阿里云百炼平台的接口与qwen-max模型
  ai:
    openai:
      api-key: ${DASHSCOPE_API_KEY}
      base-url: https://dashscope.aliyuncs.com/compatible-mode
      chat:
        options:
          model: qwen-max